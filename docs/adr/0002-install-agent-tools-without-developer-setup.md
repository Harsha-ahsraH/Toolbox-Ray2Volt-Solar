# Install agent tools without developer setup

Ray2Volt agent tools must install, upgrade, and repair through one non-admin PowerShell command on Windows 11, without requiring users to clone the repository or preinstall a global development runtime. This makes each workstation independently supportable at the cost of maintaining a versioned installer and per-harness adapter installation logic.
